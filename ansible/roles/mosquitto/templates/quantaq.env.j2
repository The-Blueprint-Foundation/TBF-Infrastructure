# /opt/air-quality/producers/quantaq/.env
# Managed by Ansible — do not edit manually.
# Secrets sourced from GCP instance metadata via Terraform.

# ── MQTT broker connection (shared baseline for all producers) ─────────────────
MQTT_HOST=localhost
MQTT_PORT={{ mqtt_port }}

# ── QuantAQ API credentials ─────────────────────────────────────────────────
QUANTAQ_API_KEY={{ quantaq_api_key }}

# Add further producer-specific variables below as needed.
